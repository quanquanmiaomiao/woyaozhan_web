/**
 * Created by Caowenjuan on 17/3/23.
 */

// 获取屏幕可见区域的宽度
export function getBodyWidth() {
  return document.body.clientWidth;
}

// 获取屏幕可见区域的高度
export function getBodyHeight() {
  return document.body.clientHeight;
}