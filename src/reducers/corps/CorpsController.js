/**
 * Created by Caowenjuan on 17/4/6.
 */
import * as Actions from './CorpsActions';
import * as Api from '../../api/corps/corps';

export function getCorpsList(params, dispatch) {
  //dispatch(Actions.corpsListRequest());
  //return Api.getCorpsList(params).then((data) => {
  //  dispatch(Actions.corpsListSuccess(data));
  //}).catch(() => {
  //  dispatch(Actions.corpsListError());
  //})
}

export function getCorpsInfo(params, dispatch) {
  //dispatch(Actions.corpsInfoRequest());
  //return Api.getCorpsInfo(params).then((data) => {
  //  dispatch(Actions.corpsInfoSuccess(data));
  //}).catch(() => {
  //  dispatch(Actions.corpsInfoError());
  //})
}

export function addCorps(params, dispatch) {
  //return Api.createCorps(params).then(() => {
  //
  //}).catch(() => {
  //
  //})
}

export function joinCorps(params, dispatch) {
  //return Api.joinCorps(params).then(() => {
  //
  //}).catch(() => {
  //
  //})
}

