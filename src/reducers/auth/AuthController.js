/**
 * Created by Caowenjuan on 16/12/1.
 */

import * as Actions from './AuthActions';
import * as Api from '../../api/auth/auth';


export function login(params, dispatch) {
  //Api.loginApi(params).then((result) => {
  //  dispatch(Actions.loginSuccess(result))
  //}).catch(() => {
  //  dispatch(Actions.loginError())
  //});
}

export function register(params, dispatch) {
  //Api.register(params).then((result) => {
  //  dispatch(Actions.registerSuccess(result))
  //}).catch(() => {
  //  dispatch(Actions.registerError())
  //});
}

